# Заданный список чисел
numbers = [1, 2, 3, 4, 5]

# Инициализация переменной для хранения суммы
total = 0

# Проход по каждому числу в списке и добавление его к общей сумме
for num in numbers:
    total += num

# Вывод суммы на экран
print("Сумма чисел в списке:", total)
